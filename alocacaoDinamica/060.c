// Alocação dinamica
// solução simples: Declarar um array bem grande

/*
- array são agrupamentos de mesmo tipo de dados
na memória.
- um ponteiro guarda o endereço de um dado na
memória
- o nome do array é um ponteiro para o primeiro
elemento do array
*/

#include <stdio.h>

/* Solicita um bloco de memória e coloca a sua
   primerira posição em um ponteiro.
   
   Uma certa quantidade de MEMÓRIA ser um ARRAY */

int main(){

    // se cria um ponteiro
    // se pede o tamanho da memória que deseja reservar
    // o ponteiro aponta para a primeira posição desse bloco de memória

    malloc
    calloc
    realloc 
    free
    
    return 0;
}